(defvar inputfile "inp")
(defvar executable "out")
(defvar temp-in ".testcase.in")
(defvar temp-out ".testcase.out")

(defun starts-with-p (str1 str2)
  "Determine whether `str1` starts with `str2`"
  (let ((p (search str2 str1)))
    (and p (= 0 p))))

Ok I'll definitely write this some day!!
maybe

(defun read-testcases ()
    
  )

